[bootstrap]
packages: bash curl md5sha1sum wget

[curl]
build-packages: brotli gsasl libidn2 libmetalink libpsl libssh2
                nghttp2 openldap openssl zstd

[development]
packages: autoconf automake libtool make pkg-config
pattern:

[dmalloc]
install-doc-packages: texi2html

[docker]
packages: docker

[docker-ce]
dependencies:
packages:

[emacs]
build-packages: ${development:packages} ${image-library:packages}
                ${render-library:packages} ${toolkit-library:packages}
                ${utility-library:packages} ${webkitgtk:packages}

[git]
build-packages: ${bootstrap:packages} ${development:packages}
                asciidoc curl expat gettext openssl xmlto zlib

[http-client]
packages:

[image-library]
packages:

[mysql]
client-packages: mariadb
server-packages: mariadb

[odbc]
development-packages:
packages:

[pgadmin4]
packages:

[postgresql]
client-packages: postgresql
contrib-packages:
devel-packages:
odbc-packages:
server-packages: postgresql-server

[python]
build-packages: ${development:packages}
                ${development-packages}
                ${library-packages}
development-packages: ${interpreter}
flask-packages:
interpreter: python
library-packages: bzip2 gdbm gettext libffi ncurses openssl
                  readline sqlite xz zlib
mysql-packages:
pip-packages:
postgresql-packages: libpq ${interpreter}
sqlalchemy-packages:
sqlite-packages:
utility-packages: pycodestyle pylint
venv-packages:
virtualenv-packages:

[render-library]
packages:

[sqlite]
packages: sqlite

[tk]
dependencies:

[toolkit-library]
packages:

[utility-library]
packages:

[uwsgi]
packages: uwsgi

[webkitgtk]
packages:
