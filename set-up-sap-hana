#!/bin/sh -eu

DATADIR=/data/sap-hana
JSONFILE=sap-hana.json

/bin/rm -rf $DATADIR
mkdir -p $DATADIR
cat <<EOF >$DATADIR/$JSONFILE
{
    "master_password" : "P455w0rd"
}
EOF
chmod u=rw,go= $DATADIR/$JSONFILE
chown -R 12000:79 $DATADIR
